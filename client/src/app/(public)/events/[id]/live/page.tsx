import { LiveWorkshopEnvironment } from "@/components/workspace/live-workshop-environment";

export default function LiveWorkshopPage() {
  return (
    <div className="h-screen w-full">
      <LiveWorkshopEnvironment />
    </div>
  );
}
